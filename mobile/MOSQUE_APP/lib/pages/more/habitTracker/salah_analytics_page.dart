import 'package:flutter/material.dart';

class SalahAnalyticsPage extends StatelessWidget {
  const SalahAnalyticsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Analytics')),
      body: const Center(
        child: Text('Weekly / Monthly / Yearly charts coming soon'),
      ),
    );
  }
}
