import 'package:flutter/material.dart';

class PrayerWordsPage extends StatelessWidget {
  const PrayerWordsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Prayer Words")),
      body: const Center(
        child: Text("Coming soon..."),
      ),
    );
  }
}
