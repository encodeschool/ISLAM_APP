enum LearningActivityType {
  multipleChoice,  // current page
  write,           // user writes the letter/word
  listen,          // audio, pick the right letter
  cardMatch,       // match Arabic -> English
  recognition,     // flashcard recognition
}
