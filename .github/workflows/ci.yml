# name: CI Pipeline

# on:
#   push:
#     branches: [ project, develop ]
#   pull_request:

# jobs:

#   # -------------------------
#   # 🟦 ADMIN (React)
#   # -------------------------
#   admin:
#     name: Admin React Build
#     runs-on: ubuntu-latest
#     if: contains(join(github.event.commits.*.modified, ','), 'admin/')

#     defaults:
#       run:
#         working-directory: admin/admin-website

#     steps:
#       - uses: actions/checkout@v4

#       - uses: actions/setup-node@v4
#         with:
#           node-version: 18
#           cache: 'npm'
#           cache-dependency-path: admin/admin-website/package-lock.json

#       - run: npm ci
#       - run: npm run build

#   # -------------------------
#   # 🌐 WEBSITE (React)
#   # -------------------------
#   website:
#     name: Website React Build
#     runs-on: ubuntu-latest
#     if: contains(join(github.event.commits.*.modified, ','), 'website/')

#     defaults:
#       run:
#         working-directory: website/farz-website

#     steps:
#       - uses: actions/checkout@v4

#       - uses: actions/setup-node@v4
#         with:
#           node-version: 18
#           cache: 'npm'
#           cache-dependency-path: website/farz-website/package-lock.json

#       - run: npm ci
#       - run: npm run build

#   # -------------------------
#   # ☕ BACKEND (Spring Boot)
#   # -------------------------
#   backend:
#     name: Backend Build
#     runs-on: ubuntu-latest
#     if: contains(join(github.event.commits.*.modified, ','), 'backend/')

#     defaults:
#       run:
#         working-directory: backend/services

#     steps:
#       - uses: actions/checkout@v4

#       - uses: actions/setup-java@v4
#         with:
#           distribution: 'temurin'
#           java-version: '17'
#           cache: 'maven'

#       - name: Build JAR
#         run: mvn clean package -DskipTests

#   # -------------------------
#   # 📱 MOBILE (Flutter)
#   # -------------------------
#   mobile:
#     name: Flutter Build
#     runs-on: ubuntu-latest
#     if: contains(join(github.event.commits.*.modified, ','), 'mobile/')

#     defaults:
#       run:
#         working-directory: mobile

#     steps:
#       - uses: actions/checkout@v4

#       - uses: subosito/flutter-action@v2
#         with:
#           flutter-version: '3.x'
#           cache: true

#       - run: flutter pub get
#       - run: flutter analyze
#       - run: flutter build apk